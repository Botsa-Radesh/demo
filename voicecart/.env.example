# Copy this to .env (or .env.local) and fill in your credentials
# Ask the team lead for the shared AWS and Gemini values

AWS_REGION=ap-south-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
DYNAMODB_TABLE=VoiceCart

NEXT_PUBLIC_GEMINI_KEY=
